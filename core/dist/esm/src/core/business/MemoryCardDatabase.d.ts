import { CardDataLoaderService } from "./CardDataLoaderService";
import { Card } from "../model/Card";
import { CardSet } from "../model/CardSet";
import { CardDatabase } from "./CardDatabase";
import { CardType } from "../model/CardType";
import { CardTypeGroup } from "../model/CardTypeGroup";
declare class MemoryCardDatabase implements CardDatabase {
    private readonly dataLoaderClient;
    private ready;
    private readonly cards;
    private readonly sets;
    private readonly types;
    private readonly races;
    private readonly monsterAttributes;
    private readonly monsterLinkMarkers;
    private readonly monsterLevels;
    constructor(dataLoaderClient: CardDataLoaderService);
    init(): Promise<void>;
    isReady(): boolean;
    hasCard(cardId: string): boolean;
    getCard(cardId: string): Card | null;
    getCards(): Card[];
    getSets(): CardSet[];
    getTypes(): CardType[];
    getRaces(cardTypeGroup: CardTypeGroup): string[];
    getMonsterAttributes(): string[];
    getMonsterLevels(): number[];
    getMonsterLinkMarkers(): string[];
    private createLinkedCard;
    private linkSets;
    private linkType;
}
export { MemoryCardDatabase };
